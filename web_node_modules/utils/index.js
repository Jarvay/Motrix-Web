import { Message } from 'element-ui'

export function notSupport () {
  Message.info('Web version do not support this function')
}

export function resolve (...args) {
  let result = ''
  for (const arg of args) {
    result += arg.startsWith('/') ? arg : `/${arg}`
  }

  return result
}
